---
title: "Argilla integrations"
description: "Integrate with Argilla using LangChain Python."
---

>[Argilla](https://argilla.io/) is an open-source data curation platform for LLMs.
> Using `Argilla`, everyone can build robust language models through faster data curation
> using both human and machine feedback. `Argilla` provides support for each step in the MLOps cycle,
> from data labeling to model monitoring.

## Installation and setup

Get your [API key](https://platform.openai.com/account/api-keys).

Install the Python package:

<CodeGroup>
```bash pip
pip install argilla
```

```bash uv
uv add argilla
```
</CodeGroup>

## Callbacks


```python
from langchain.callbacks import ArgillaCallbackHandler
```

See an [example](/oss/integrations/callbacks/argilla).
